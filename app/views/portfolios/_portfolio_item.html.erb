  <!-- <p><%= link_to portfolio_item.title,portfolio_path(portfolio_item) %></p>
   
   <p><%= portfolio_item.subtitle%></p>
   
   <p><%= portfolio_item.body%></p>
   
    <%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>
   
    <%= link_to 'Edit', edit_portfolio_path(portfolio_item) %><br>
   
    <%= link_to 'Delete', portfolio_path(portfolio_item),method: :delete, data: { confirm: 'Are you sure?' } %>
-->
<div class="col-md-4">
   <div class="card mb-4 shadow-sm">
   <%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>
   <div class="card-body">
      <p class="card-title">  <%= link_to portfolio_item.title,portfolio_path(portfolio_item) %> </p>
      <p class="card-subtitle"><%= portfolio_item.subtitle%></p>
   </div>
  </div>
</div>