<div class="container">
    <div class="row"> 
        <div class="col-md-7">
         <%= image_tag @portfolio_item.main_image%> 
        </div>
        <div class="col-md-5">
            <h1> <%= @portfolio_item.title%> </h1>
            <hr>
            <em> <%= @portfolio_item.subtitle %> </em>
            <hr>

            <h5 class="lead"> <%= @portfolio_item.body %></h3>
        <h3>Technologies used</h3>
        <% @portfolio_item.technologies.each do |t|%>
        <p><%= t.name %></p>
        <% end %>
       
         </div>
    </div>
</div>